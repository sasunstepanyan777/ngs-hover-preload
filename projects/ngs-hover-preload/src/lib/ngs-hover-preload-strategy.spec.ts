import { NgsHoverPreloadStrategy } from './ngs-hover-preload-strategy';

describe('NgsHoverPreloadStrategy', () => {
  it('should create an instance', () => {
    expect(new NgsHoverPreloadStrategy()).toBeTruthy();
  });
});
