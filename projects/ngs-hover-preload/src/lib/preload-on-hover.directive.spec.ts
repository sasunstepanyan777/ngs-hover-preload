import { PreloadOnHoverDirective } from './preload-on-hover.directive';

describe('PreloadOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
